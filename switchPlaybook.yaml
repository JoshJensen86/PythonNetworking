---
- name: Create Local-Admin user on ExtremeXOS switches using raw module
  hosts: all
  gather_facts: no
  tasks:
    - name: Create Local-Admin user with admin privileges
      raw: |
        create account Local-Admin password WGU123 privilege 15
        save configuration
